/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains : [
            "img.aviationpros.com", 
            "imgv3.fotor.com",
            "thumbs.dreamstime.com"
        ]
    }
};

export default nextConfig;
